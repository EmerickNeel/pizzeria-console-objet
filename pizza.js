class Pizza {

    id;
    code;
    libelle;
    prix;

    constructor(id, code, libelle, prix) {
        this.id = id;
        this.code = code;
        this.libelle = libelle;
        this.prix = prix;
    }
}